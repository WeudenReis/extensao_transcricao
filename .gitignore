# Dependências
node_modules/

# Builds e pacotes
dist/
server/dist/

# Ambiente e segredos — NUNCA commitar
.env
.env.*
!.env.example
.env.local
*.pem
*.key

# Banco de dados local
server/data/
*.db
*.sqlite

# Logs e temporários
*.log
.DS_Store
Thumbs.db

# Cérebro visual gerado pelo Graphify (node scripts/gerar-grafo.mjs).
# São ~2,3 MB regenerados a cada execução: versionar só criaria diffs
# gigantes a cada mudança de código.
grafo/
